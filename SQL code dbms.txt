CREATE DATABASE EATOO;
USE EATOO;
CREATE TABLE Students (
    student_id INT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(50) NOT NULL,
    email VARCHAR(100) UNIQUE,
    phone VARCHAR(15)
);

CREATE TABLE Vendors (
    vendor_id INT PRIMARY KEY AUTO_INCREMENT,
    vendor_name VARCHAR(50) NOT NULL,
    location VARCHAR(50)
);

CREATE TABLE Food_Items (
    food_id INT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(50) NOT NULL,
    price DECIMAL(6,2) CHECK (price > 0),
    vendor_id INT,
    FOREIGN KEY (vendor_id) REFERENCES Vendors(vendor_id)
);

CREATE TABLE Orders (
    order_id INT PRIMARY KEY AUTO_INCREMENT,
    student_id INT,
    order_date DATE DEFAULT CURRENT_DATE,
    total_amount DECIMAL(8,2),
    FOREIGN KEY (student_id) REFERENCES Students(student_id)
);

CREATE TABLE Order_Details (
    order_detail_id INT PRIMARY KEY AUTO_INCREMENT,
    order_id INT,
    food_id INT,
    quantity INT CHECK (quantity > 0),
    FOREIGN KEY (order_id) REFERENCES Orders(order_id),
    FOREIGN KEY (food_id) REFERENCES Food_Items(food_id)
);

CREATE TABLE Reviews (
    review_id INT PRIMARY KEY AUTO_INCREMENT,
    student_id INT,
    food_id INT,
    rating INT CHECK (rating BETWEEN 1 AND 5),
    comment VARCHAR(255),
    FOREIGN KEY (student_id) REFERENCES Students(student_id),
    FOREIGN KEY (food_id) REFERENCES Food_Items(food_id)
);

CREATE TABLE Payments (
    payment_id INT PRIMARY KEY AUTO_INCREMENT,
    order_id INT,
    payment_mode VARCHAR(20),
    status VARCHAR(20) DEFAULT 'Pending',
    FOREIGN KEY (order_id) REFERENCES Orders(order_id)
);

INSERT INTO Students(name,email,phone) VALUES
('Ram','ram@gmail.com','9999999999'),
('Amit','amit@gmail.com','8888888888');

INSERT INTO Vendors(vendor_name,location) VALUES
('Cafe Hub','Block A'),
('Food Corner','Block B');

INSERT INTO Food_Items(name,price,vendor_id) VALUES
('Burger',120,1),
('Pizza',200,1),
('Momos',80,2),
('Sandwich',90,2);

INSERT INTO Orders(student_id,total_amount) VALUES
(1,0),(2,0);

INSERT INTO Order_Details(order_id,food_id,quantity) VALUES
(1,1,2),
(1,3,1),
(2,2,1);

INSERT INTO Reviews(student_id,food_id,rating,comment) VALUES
(1,1,5,'Excellent'),
(2,2,4,'Good'),
(1,3,3,'Average');

SELECT * FROM Students;
SELECT * FROM Vendors;
SELECT * FROM Food_Items;
SELECT * FROM Orders;
SELECT * FROM Order_Details;
SELECT * FROM Reviews;

SELECT s.name, f.name, v.vendor_name
FROM Students s
JOIN Orders o ON s.student_id = o.student_id
JOIN Order_Details od ON o.order_id = od.order_id
JOIN Food_Items f ON od.food_id = f.food_id
JOIN Vendors v ON f.vendor_id = v.vendor_id;